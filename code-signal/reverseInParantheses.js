/*
Write a function that reverses characters in
(possibly nested) parentheses in the input string.
*/

/*
create container
loop through the array
if reach '(' then send chars up until ')' or '(' to container
once you reach another paren, flatten the array into a string
*/
